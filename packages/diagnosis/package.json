{
  "name": "@aapsd/diagnosis",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@aapsd/contracts": "*",
    "@aapsd/policy": "*"
  },
  "devDependencies": {
    "vitest": "^2.0.0"
  }
}
